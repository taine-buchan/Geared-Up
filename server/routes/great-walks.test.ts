import { vi, describe, it, expect } from 'vitest'
import request from 'supertest'
import server from '../server'
import * as db from '../db/great-walks'

describe('GET /api/v1/great-walks', () => {
  it('should return 200 with a great walk', async () => {})
})
